#!/bin/bash

{{ if eq .chezmoi.osRelease.idLike "debian" }}
  if ! sheldon="$(command -v sheldon)"; then
      . "$HOME/.cargo/env"
      cargo install sheldon
      sudo chown -R $(whoami) /usr/local/share/zsh
  fi
{{ end -}}
